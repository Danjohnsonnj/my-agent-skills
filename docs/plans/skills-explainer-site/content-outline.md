# Content outline - Skills explainer site

Decision locks in decisions.md. Overview copy only; behavior from README / SKILL.md.

**Structure:** single `docs/index.html` with section anchors.  
**Build input for Phase 6** (after Phases 3–5).

---

## Global chrome

- Suite name `my-agent-skills`; nav to `#lifecycle` `#skills` `#install` `#principles`; secondary GitHub
- Out: dashboard clutter

## 1. Hero (`#top`)

- Brand name as hero-level signal; one headline; one supporting line that names the pain + promise
- Primary CTA: Install → `#install`; Secondary: View on GitHub
- Optional illustration slot (fast-follow)

## 2. Lifecycle skim (`#lifecycle`) — main “get it” moment

- Hybrid skim strip: four steps with verb labels + one-liners (start-interview → plan-build → review-plan → orchestrate-build / decide → write → check → drive)
- Short narrative under/ beside strip; pain folded in here / hero, no standalone Problem section
- Optional support illustration (non-blocking)

## 3. How they connect (`#connect`)

- Throughline: explicit handoffs; `HANDOFF.md` as unit of continuity; cold-start resumability
- One beat on lessons + human-gated harvest (not a tutorial)

## 4. Skills (`#skills`)

Compact **equal** blocks for each:

| Skill | Role line |
|---|---|
| start-interview | Decide (starting point — relentless decision interview) |
| plan-build | Write & maintain |
| review-plan | Check |
| orchestrate-build | Drive (optional / opt-in once a plan exists) |

Each: what / when / why / how to start + link to SKILL.md on GitHub.

## 5. Install (`#install`)

- README-identical symlink snippet; copy button; warn not to use `~/.cursor/skills-cursor/`

## 6. Principles (`#principles`)

- Three README principles, one sentence each
- One line: human-gated harvest / lessons → suite improves only via reviewed proposals
- Links: SCRIPTS.md, README

## 7. Footer

- GitHub; quiet link: “How this site was planned” → `plans/skills-explainer-site/HANDOFF.md`
- Nav echoes

## Illustration prompt pack (Design lock; fast-follow assets)

1. Hero — cold-start / continuity metaphor (dark, clean)
2. Lifecycle — four-step support visual
3. Optional — harvest / lessons accretion

## Verify

- [x] Single-page IA locked
- [x] Section jobs locked
- [x] Ready as Phase 6 input (after 3–5)
