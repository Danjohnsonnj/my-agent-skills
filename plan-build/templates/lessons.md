# Lessons (reusable toolkit; accreted across sessions)

<!-- Durable, reusable gotchas and script/verify outcomes worth carrying forward.
     Distinct from progress-log.md (dated, append-only history): this file is the
     CURATED toolkit a future cold-start agent reuses. Add a lesson when it would
     save the next session real work; prune one when it stops being true.
     agentmemory MCP (if present) is an optional enhancement - this file is the
     portable, self-contained record and the source of truth. -->

## {{SHORT_LESSON_TITLE}}

- Context: {{WHERE_THIS_APPLIES}}
- Lesson: {{WHAT_TO_DO_OR_AVOID}}
- Evidence: {{SCRIPT_OR_VERIFY_OUTCOME — e.g. "checkpoint.sh OK", "tests FAIL exit=1", "inference fallback used because execution unavailable"}}
- Crystallize?: {{candidate script/step to capture this, or "no"}}
